#include "main.h"
/**
 * _alpha - check if it is alphabet
 *@c: char to be checked

 * Return: 1 for alphabet else 0
 */
int _isalpha(int c)
{
	int isalpha(int c);

	if (isalpha(c))
	{
		return (1);
	}
	else 
	{
		return (0);
	}
}
